# Vue Frontend

Vue 3 + Vite SPA that pairs with the FastAPI backend.

## Commands

```bash
pnpm install
pnpm dev
pnpm build
pnpm lint
pnpm test:unit
```

Set `VITE_API_BASE_URL` in `.env` (defaults to `http://localhost:8000/api`).
